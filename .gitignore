# Local .terraform directories
/TeoSchool/**/.terraform/*

# .tfstate files
/TeoSchool/*.tfstate
/TeoSchool/*.tfstate.*

# Crash log files
/TeoSchool/crash.log
/TeoSchool/crash.*.log
/TeoSchool/sallby.pem

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
#*.tfvars
#*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
/TeoSchool/override.tf
/TeoSchool/override.tf.json
/TeoSchool/*_override.tf
/TeoSchool/*_override.tf.json

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
/TeoSchool/.terraformrc
/TeoSchool/.terraform
/TeoSchool/terraform.rc
/TeoSchool/kubeconfig

# Local .terraform directories
/terraform/**/.terraform/*

# .tfstate files
/terraform/*.tfstate
/terraform/*.tfstate.*

# Crash log files
/terraform/crash.log
/terraform/crash.*.log
/terraform/sallby.pem

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
#*.tfvars
#*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
/terraform/override.tf
/terraform/override.tf.json
/terraform/*_override.tf
/terraform/*_override.tf.json

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
/terraform/.terraformrc
/terraform/.terraform
/terraform/terraform.rc
/terraform/kubeconfig


/storage-tfstate/.terraform

# Local .terraform directories
/storage-tfstate/**/.terraform/*

# .tfstate files
/storage-tfstate/*.tfstate
/storage-tfstate/*.tfstate.*

# Crash log files
/storage-tfstate/crash.log
/storage-tfstate/crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
/storage-tfstate/*.tfvars
/storage-tfstate/*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
/storage-tfstate/override.tf
/storage-tfstate/override.tf.json
/storage-tfstate/*_override.tf
/storage-tfstate/*_override.tf.json

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
/storage-tfstate/.terraformrc
/storage-tfstate/terraform.rc
